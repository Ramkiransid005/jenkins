name: maven job

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]


jobs:
  Build:
    name: Build
    runs-on: [ self-hosted, siddu ]
    steps:
      - uses: actions/checkout@v2
      - name: Build with Maven
        run: mvn -f pom.xml clean package