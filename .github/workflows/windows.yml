name: maven job

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]


jobs:
  Build:
    name: Build
    runs-on: [ self-hosted, siddhu ]
    steps:
      - uses: actions/checkout@v2
      - name: Build with Maven
        run: mvn -f pomx.ml clean package